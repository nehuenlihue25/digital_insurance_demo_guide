# macOS
.DS_Store
._*

# Claude Code session state (privado)
.claude/
launch.json
scheduled_tasks.json

# Salesforce CLI state
.sfdx/
.sf/

# MCP config — contains injected SF_ACCESS_TOKEN, must never be committed.
# Every user generates their own via demo-metadata/scripts/setup-omnistudio-mcp.sh
.mcp.json

# Temp files
*.tmp
*.swp
*~
